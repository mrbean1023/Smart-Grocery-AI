/**
 * Compatibility re-export — the canonical implementation lives in
 * common/decorators. Both import paths resolve to the same decorator.
 */
export { CurrentUser } from '../../common/decorators/current-user.decorator';
