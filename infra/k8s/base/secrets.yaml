# =============================================================================
# PLACEHOLDER SECRETS — do NOT put real values in git.
# In real environments manage this Secret with External Secrets Operator
# (backed by AWS Secrets Manager — see infra/terraform/rds.tf) or
# Sealed Secrets, and remove this file from the kustomization.
# =============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  labels:
    app.kubernetes.io/part-of: smart-grocery
type: Opaque
stringData:
  DATABASE_URL: "CHANGE_ME"
  REDIS_URL: "CHANGE_ME"
  JWT_ACCESS_SECRET: "CHANGE_ME"
  JWT_REFRESH_SECRET: "CHANGE_ME"
  NEXTAUTH_SECRET: "CHANGE_ME"
  OPENAI_API_KEY: "CHANGE_ME"
  STRIPE_SECRET_KEY: "CHANGE_ME"
  STRIPE_WEBHOOK_SECRET: "CHANGE_ME"
  STRIPE_PRICE_PREMIUM_MONTHLY: "CHANGE_ME"
  STRIPE_PRICE_PREMIUM_YEARLY: "CHANGE_ME"
  SMTP_HOST: "CHANGE_ME"
  SMTP_PORT: "CHANGE_ME"
  SMTP_USER: "CHANGE_ME"
  SMTP_PASS: "CHANGE_ME"
  AWS_ACCESS_KEY_ID: "CHANGE_ME"
  AWS_SECRET_ACCESS_KEY: "CHANGE_ME"
  GOOGLE_CLIENT_ID: "CHANGE_ME"
  GOOGLE_CLIENT_SECRET: "CHANGE_ME"
